// src/three/postprocessing.js — Bloom + Color Grading
// Will be implemented in Phase 2 with EffectComposer

export function initPostProcessing(renderer, scene, camera) {
  // Placeholder — bloom + color grading setup
  console.log('[PostProcessing] Ready for Phase 2 implementation')
  return null
}
